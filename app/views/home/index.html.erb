<div class="container">
  <div class="row">
    <nav class="navbar navbar-expand-lg">
      <a class="navbar-brand pl-2" href="#">CATEGORIES</a>
      <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
        <div class="navbar-nav">
          <% @categories.each do |category| %> 
            <%= link_to category.name, category_path(category), class: "nav-item nav-link active" %><span class="sr-only">(current)</span>
          <% end %>
            <%= link_to "View all", categories_path, class: "nav-item nav-link active" %>
        </div>
      </div>
    </nav>
  </div>
  <div class="row mt-4">
    <div class="col">
      <p class="h4">Popular Products</p>
    </div>
  </div>
  <div class="row">
    <div class="col-8">
      Left col
    </div>
      
    </div>
  </div>
</div>